{
  "comment": "Public leaves that ship with a real implementation even though no roster repo imports them yet. The stub list is consumer-driven, so a primitive published for the fleet to adopt is stubbed on its first release \u2014 and a stubbed leaf cannot acquire the consumer that would un-stub it. Each entry names the consumer it waits for. Remove an entry once that consumer lands; the audit reports the entry as redundant.",
  "leaves": [
    {
      "leaf": "process/spawn/retry/browser",
      "reason": "The browser retry variant. Ships with its Node twin so the export alias resolves in both conditions."
    },
    {
      "leaf": "process/spawn/retry/node",
      "reason": "The spawn retry policy. Ships ahead of its first consumer: the wheelhouse GHCR listing collapses onto it once this releases."
    },
    {
      "leaf": "process/spawn/retry/policy",
      "reason": "The shared retry decisions. Both platform variants re-export it."
    },
    {
      "leaf": "process/spawn/with-retry",
      "reason": "Async spawn with retry. Ships ahead of its first consumer, same as the sync path."
    },
    {
      "leaf": "process/tree",
      "reason": "readProcessTree and the self-nesting check. Waits on socket-wheelhouse's stale-process-sweeper replacing its private copy with this one."
    },
    {
      "leaf": "yaml/edit",
      "reason": "EditableYaml. Waits on the fleet migration of regex-based pnpm-workspace.yaml edits to a comment-preserving editor."
    },
    {
      "leaf": "yaml/types",
      "reason": "The injected-parser types for yaml/edit. Ships with its implementation leaf."
    }
  ]
}
