name: 📦 Publish to npm

# Dependencies:
#   - SocketDev/socket-registry/.github/actions/setup-and-install

# Publishing is INLINED here (not delegated to socket-registry's reusable
# provenance.yml) on purpose. npm's OIDC trusted-publisher matcher keys on the
# workflow that contains the publish step. When the publish step lives in a
# reusable workflow, the OIDC `job_workflow_ref` claim carries the reusable's
# SHA-pinned ref — which changes every time we cascade a new socket-registry
# SHA, so no stable npm trusted-publisher config can match it (the token
# exchange 404s → publish runs unauthenticated → PUT 404 / stage 403).
#
# Inlining keeps `job_workflow_ref` = SocketDev/socket-lib/.github/workflows/
# provenance.yml@<ref>, a stable identity the trusted publisher matches.

on:
  workflow_dispatch:
    inputs:
      dry-run:
        description: 'Dry run (default: true)'
        required: false
        default: true
        type: boolean

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false # Don't cancel publishing.

jobs:
  publish:
    name: Build and Publish
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      # Needed for npm provenance via OIDC trusted publishing.
      id-token: write
    steps:
      - uses: SocketDev/socket-registry/.github/actions/setup-and-install@3db17f891a466d217eadc61da9d33093047f2015 # main (2026-05-28)
        with:
          socket-api-token: ${{ secrets.SOCKET_API_TOKEN || secrets.SOCKET_API_KEY }}

      - name: Build
        run: pnpm run build

      - name: Publish to npm
        # TEMPORARY: --direct (classic-token publish, no staging) instead of
        # --staged because Socket Firewall does not yet allowlist the
        # /-/stage/* endpoints, so `pnpm stage publish` fails with
        # "failed to parse URL" + 403. Tracked in firewall PR #123. Flip
        # back to --staged once that lands + deploys.
        env:
          DRY_RUN: ${{ inputs.dry-run }}
        run: |
          if [ "$DRY_RUN" = "true" ]; then
            node scripts/publish.mts --direct --dry-run
          else
            node scripts/publish.mts --direct
          fi
