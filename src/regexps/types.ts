/**
 * @fileoverview Public type surface for `regexps/*` modules — currently
 * no exported types. Reserved for future regex helper option records.
 * Pure types, no runtime side effects.
 */

export {}
