/**
 * @fileoverview Public type surface for `warnings/*` modules.
 * Empty by design — the module exports functions, not types. Kept
 * for consistency with the hybrid naming convention. Pure types, no
 * runtime side effects.
 */
