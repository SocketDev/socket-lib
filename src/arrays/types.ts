/**
 * @fileoverview Public type surface for `arrays/*` modules. Empty
 * by design — the module exports functions and a single isArray
 * predicate, no public types. Kept for consistency with the hybrid
 * naming convention. Pure types, no runtime side effects.
 */


