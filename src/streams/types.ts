/**
 * @fileoverview Public type surface for `streams/*` modules — currently
 * re-exports nothing; stream helpers borrow `IterationOptions` from
 * `promises/types`. Pure types, no runtime side effects.
 */


