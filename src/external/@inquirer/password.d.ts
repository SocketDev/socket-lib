/**
 * @file Type surface for the vendored `@inquirer/password` bundle beside it —
 *   the real package's types, re-exported so an extensionless import of
 *   `../external/@inquirer/password` resolves for tsc the way the runtime
 *   resolves the sibling `.js`.
 */

export { default } from '@inquirer/password'
