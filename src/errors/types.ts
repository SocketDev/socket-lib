/**
 * @fileoverview Public type surface for `errors/*` modules — currently
 * re-exports nothing; helpers narrow to the standard `Error` /
 * `NodeJS.ErrnoException` types directly. Pure types, no runtime
 * side effects.
 */


